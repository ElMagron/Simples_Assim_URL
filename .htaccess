<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    # Redireciona todas as requisições não-existentes para o index.php
    RewriteRule ^ index.php [L]
</IfModule>